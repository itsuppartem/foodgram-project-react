from django.apps import AppConfig


class FoodgramConfig(AppConfig):
    """
    Announcing that we have app, named FOODGRAM.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "foodgram"
